# A_Share_Agent 量化终端功能完善与编码修复说明书

本说明文档记录了针对 `A_Share_Agent` 系统所进行的功能升级、Bug 修复以及针对 Windows 环境的编码兼容性改造工作。

---

## 🛠️ 核心变更与优化内容

### 1. 🐞 修复多智能体分析引擎导入 Bug
- **涉及文件**：`analysts.py`
- **原因分析**：启动时无法从 `config` 中直接取得 `client` 实例，导致深度扫描时多智能体联席会商启动失败。
- **优化方案**：修改为从初始化函数按需获取 OpenAI 客户端对象：
  ```python
  from config import get_openai_client
  client = get_openai_client()
  ```
- **实际效果**：成功解决运行时引用冲突，打通了“技术面、舆情面、基本面、风险面” 4 位虚拟分析师联合会商的调用链路。

### 2. 🗄️ 模拟持仓由本地 JSON 升级至 SQLite 数据库
- **涉及文件**：`portfolio.py`、`database.py`
- **优化方案**：
  - 构建关系型数据库表 `portfolio` 管理持仓，废弃并替换了读写本地 `portfolio_data.json` 的传统模式。
  - 在 `database.py` 中实现了**无缝热迁移机制**：若系统检测到本地有旧的 `portfolio_data.json` 且数据库中为空，会自动将旧数据解析并录入 SQLite 数据库。
- **实际效果**：大大提高了持仓数据并发读写的安全性和事务一致性。

### 3. 📝 补全 Agent 工具调用日志的持久化存储
- **涉及文件**：`agent_tools.py`、`database.py`
- **优化方案**：
  - 在 ReAct 智能体主循环 `run_agent` 执行工具调用（如 RAG 检索、行情获取等）时，实时将工具名称、传入参数及返回值预览信息保存入 `agent_logs` 数据表中。
- **实际效果**：为 Agent 的底层决策链路提供了全自动化的审计日志，破除了 AI 的“黑盒子”隐患。

### 4. 👥 整合“多智能体联席研判”及共识展现前端
- **涉及文件**：`app.py`
- **优化方案**：
  - 在“🔍 单股深度扫描”页面的右半部分（col2）引入 `st.tabs` 选项卡，区分“独立深度研报”与“多智能体联席研判”。
  - 开发了 **“⚡ 启动多智能体联席会商”** 功能：点击后多智能体依次发表意见，系统最终根据权重合力算出共识评级（看多/看空/中性）与综合置信度，并将研报 JSON 结构化存储入库。
  - 运用精心调配的高端暗黑风格 HTML 卡片组展现 4 位分析师的研判结论，让结论信息一眼即知。

### 5. 📜 打造“研报历史记录”追溯页面
- **涉及文件**：`app.py`
- **优化方案**：
  - 在左侧控制面板增加 **“📜 研报历史记录”** 菜单。
  - 支持从 `analysis_history` 表中查询历史的会商档案，并根据所选的历史研报，动态重现当时的联席共识 Banner 以及 4 位分析师的详细评分、论据展开详情。

### 6. 🚀 一劳永逸地解决 Windows 终端 Emoji 编码崩溃问题
- **涉及文件**：`app.py`、`database.py`、`agent_test.py`
- **原因分析**：Windows 系统控制台默认编码通常为 `GBK`，当 Python 执行含有 Emoji 的 `print(...)` 语句时，会引发 `UnicodeEncodeError` 崩溃。Streamlit 运行进程中断后会导致前端 DOM 残留，形成 UI 界面排版错乱和元素重复的乱象。
- **优化方案**：在所有入口脚本和数据库操作的最顶部加入标准输入输出流的 UTF-8 重定向及异常过滤机制：
  ```python
  import sys
  try:
      if sys.platform.startswith('win'):
          sys.stdout.reconfigure(encoding='utf-8', errors='replace')
          sys.stderr.reconfigure(encoding='utf-8', errors='replace')
  except Exception:
      pass
  ```
- **实际效果**：使得系统即使在未显式配置 `set PYTHONUTF8=1` 的普通命令行环境下也能稳定运行，不再因任何 Emoji 或中文字符解析异常而导致后台奔溃。


### 7. 🌍 永久配置整机 Windows Python 编码环境（UTF-8）
- **优化方案**：
  - 通过 PowerShell 执行系统指令，在 Windows 用户注册表永久写入 `PYTHONUTF8=1` 与 `PYTHONIOENCODING=utf-8` 环境变量。
- **实际效果**：
  - 从根本上、全局性地解决了您本机运行**所有** Python 项目时，因为 Emoji 或中文字符在 `GBK` 终端输出时导致 `UnicodeEncodeError` 的报错崩溃问题。所有新打开的控制台或 VS Code 终端均会自动继承此 UTF-8 运行模式。

### 8. 🛡️ 针对智谱 AI (GLM) 频率限制 (429/1302) 的双重容错与架构优化
- **涉及文件**：`vector_rag.py`、`app.py`
- **优化方案**：
  - **请求数减半（架构层）**：重构了 `vector_rag.py` 中的 `rag_retrieve` 函数。将“用户 Query”和“新闻文本切片”一并放入一个列表发送，**由 2 次网络 API 调用合并为 1 次批量 Embedding 传输**，网络交互开销直降 50%。
  - **防抖延迟机制（流控层）**：在 `app.py` 中循环遍历多支股票时，引入了 `1.2` 秒的安全等待延迟（`time.sleep(1.2)`），确保在多股连扫时请求频次绝不会超越智谱低阶账户的 `2 QPS` 硬性水位线。
  - **指数退避重试**：在 API 调用捕获异常时，加入 3 次指数增长的自动避让重试，防止突发流量崩溃。
  - **异常友好交互**：在 UI 报错层定制化捕捉 `1302`/`429` 错误并给出了明确的大模型平台余额和额度排查提示。

---

## 🧪 功能验证
- 所有修改代码均已通过 `python -m py_compile` 语法编译检查，无语法和缩进错误。
- 数据库建表、旧持仓数据热迁移及 Agent 日志落库逻辑均已运行独立脚本成功测试。
- Streamlit 服务器已在后台顺利载入运行。
